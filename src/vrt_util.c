#include <vrt/vrt_util.h>

/* Defined inline in header */
extern bool vrt_is_context(vrt_packet_type type);
extern bool vrt_has_stream_id(vrt_packet_type type);
extern bool vrt_is_platform_little_endian(void);
